# Runtime dependencies (installed into the container image).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pypdf==5.1.0
python-multipart==0.0.20
prometheus-fastapi-instrumentator==7.0.0   # /metrics (RED) for canary verification
